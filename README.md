# Plana

3D CAD для планировок. Монорепа без промежуточной сборки пакетов: Next.js транспилирует `@plana/*` из исходников.

```
apps/web → editor → viewer → renderer → core
```

Демо: https://xlebpushek.github.io/plana/

```bash
pnpm install
pnpm dev
```
