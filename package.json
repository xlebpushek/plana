{
  "name": "plana",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "pnpm -r --filter=./packages/* build",
    "test": "pnpm --filter @plana/core test",
    "typecheck": "pnpm -r typecheck",
    "dev": "pnpm --filter @plana/editor dev"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  }
}
